Subject Column Property Annotation
---
post:
  tags:
  - "subject"
  summary: "This is the summary"
  description: "Optional extended description in Markdown."
  consumes:
  - "multipart/form-data"
  produces:
  - "application/json"
  parameters:
    - in: "formData"
      name: "col_id"
      type: "string"
      required: true
  responses:
    200:
      description: OK